<%- include('partials/header'); -%>
<h1>Home</h1>
<p><%= varr1 %></p>

<%# for(var i = 0 ; i < posts.length ; i++){ %>
 <%# console.log(posts[i].title); %>
 <%# } %>

 <% posts.forEach(function(post) { %>
  <%# console.log(post.title); %>
  
<h1><%= post.title %></h1>
<!-- here we are truncating string using javascript -->
<% var truncated_str = post.content.substring(0,Math.min(100 , post.content.length)) ; %>
<p><%= truncated_str %>... <a href = "/posts/<%=post.title%>">Read More</a></p>

<% }); %>

<%- include('partials/footer'); -%>
